apply plugin: 'java'
apply from: 'gradle/dependencies.gradle'
apply plugin: 'application'
repositories {
	mavenCentral()
	mavenLocal()
}

mainClassName = "org.jboss.weld.environment.se.StartMain"

dependencies {
	compile libraries.logging.api,
			libraries.weld
	runtime libraries.logging.impl

	testCompile libraries.testing.jUnitAndMockito
}
